%article.articles
	%section.intro
		%h1.page-title That's Some Sharp Thinking.

		%p.lede
			We're passionate about our jobs and have an Agile mindset. Read on for informative views on technology, process, and teamwork.

	= partial "latest-posts"

	%section.older-posts
		-#%h1 Older Articles

		-#.posts
			- blog.articles[3, blog.articles.length].each do |article|
				.post-summary
					= link_to article.url, :class => article.data.colour do
						.post-date
							%time{:datetime => article.date.strftime("%B %e, %Y")} #{article.date.strftime("%e<br>%b")}

						%h2.post-title= article.title

		%h1 More articles coming soon!
		%p #{link_to "Subscribe to the feed", "/feed.xml"}
